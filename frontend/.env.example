# Environment Configuration for CSAO Frontend

# Replace this with your actual Render backend URL once deployed.
# Ensure it ends with /api to match the FastAPI routing.
VITE_API_URL=https://your-backend-app-name.onrender.com/api
